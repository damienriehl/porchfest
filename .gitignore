node_modules/
dist/
/drizzle/
data/
*.db
*.db-shm
*.db-wal
.env
.env.*
!.env.example

# Public-repository privacy boundary. The clean-room scan enforces these in Git
# history and container payloads as well as relying on ignore rules.
private/
out/
raw-exports/
generated-messages/
*.csv
*.tsv
*.xls
*.xlsx
*.ods
*.eml
*.mbox

# Scratch output from codex-run.sh, which requires its out-file inside the repo.
# The clean-room scanner still reads ignored paths, so this hides nothing from it.
.codex-out/
