# 2026-09-02 UAT fixes report

## Outcome

All twelve validated UAT findings are implemented in the `uat-2026-09-02` worktree and covered by focused tests. The complete runnable suite passes: 1000 tests across 51 files, excluding only the SMTP tests that cannot open a loopback listener in this sandbox.

The requested commits could not be created. This workspace can edit the worktree but the linked Git metadata is mounted read-only; `git add` fails while creating `.git/worktrees/porchfest-uat2/index.lock` with `Read-only file system`. The worktree is therefore intentionally left uncommitted and dirty. Nothing was pushed or merged.

## Item report

| Item | Change                                                                                                                                                                                                                                                       | Principal files                                                                                                                                                          | Proving test                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1   | Added authenticated organizer invite creation, absolute copyable links, optional address binding, redemption-time email collection, and lifecycle coverage for single use and issuer deactivation.                                                           | `packages/core/src/access.ts`, `packages/web/src/routes/admin.ts`, `packages/web/src/views/admin-shell.ts`                                                               | `packages/web/test/auth.test.ts` - `issues a copyable invite that is single-use and dies with its issuer`; `packages/core/test/access.test.ts` - unbound, expiry, hashing, audit, replay, and deactivation cases    |
| F2   | Persisted a versioned immutable original-submission snapshot for contacts, venues, and acts; rendered it read-only on edited records; preserved it through placeholder promotion and removed it during anonymization.                                        | `packages/core/drizzle/0019_familiar_genesis.sql`, `packages/core/src/original-submission.ts`, `packages/core/src/records.ts`, `packages/web/src/views/admin-records.ts` | `packages/web/test/admin-records.test.ts` - `leaves the original submission readable after an edit`; `packages/core/test/schema.test.ts` - nullable upgrade; promotion and retention tests                          |
| F3   | Displayed participant-reported shared-member text in venue-first and act-first assignment flows, including assigned slots, with a direct path to the existing Link acts form. No automatic linkage is inferred.                                              | `packages/web/src/views/shared-member-prompt.ts`, `packages/web/src/views/assign-act.ts`, `packages/web/src/views/assign-venue.ts`                                       | `packages/web/test/assign.test.ts` - `requires and records a shared-member override` covers both prompts, one-click linking, assigned view, and the resulting 409 conflict                                          |
| F4   | Added organizer coordinate entry/review forms for deployments without geocoding, using the existing server verification route and season bounding-box enforcement.                                                                                           | `packages/web/src/routes/coordinates.ts`, `packages/web/src/views/coordinates.ts`                                                                                        | `packages/web/test/coordinates.test.ts` - `offers manual coordinate entry with no geocoder and verifies an inside-box pin`; `refuses an outside-box pin and names the box`                                          |
| F5   | Added an unconfigured same-origin mutation fallback without weakening configured `PUBLIC_BASE_URL` enforcement. Host-bearing fallback requests require browser same-origin Fetch Metadata; hostile paired Host/Origin requests are refused.                  | `packages/web/src/router/registry.ts`, `packages/web/src/app.ts`                                                                                                         | `packages/web/test/boot-regressions.test.ts` - fresh bootstrap plus trusted-proxy invite URL; `packages/web/test/route-registry.test.ts` - configured/unconfigured, hostile Host, missing/cross-site Fetch Metadata |
| F6   | Documented configurable HTTP/HTTPS port mappings, Caddy/public-origin behavior, nonstandard ports, and the first-season path through `draft` to `signups_open`; aligned placeholder-secret guidance and zero-provider coordinate claims with implementation. | `README.md`, `.env.example`, `packages/web/src/config/session-secret.ts`                                                                                                 | `packages/web/test/session-secret.test.ts`; documentation format check                                                                                                                                              |
| F7   | Constrained map/page width at phone sizes and made wide filter content scroll inside its own container.                                                                                                                                                      | `packages/map/assets/porchfest-map.css`                                                                                                                                  | `packages/map/test/porchfest-map.test.js` - 375px/responsive overflow contracts                                                                                                                                     |
| F8   | Added a generated neutral SVG favicon, a public favicon route, and favicon markup.                                                                                                                                                                           | `packages/web/assets/favicon.svg`, `packages/web/src/routes/map.ts`, `packages/web/src/app.ts`                                                                           | `packages/web/test/map.test.ts` - favicon response, type, caching, SVG body, and page link                                                                                                                          |
| F9   | Made participant self-service discoverable from the public landing page and signup receipts only when the email-backed feature is enabled; kept disabled mode honest.                                                                                        | `packages/web/src/app.ts`, `packages/web/src/routes/signup.ts`, `packages/web/src/views/signup-view.ts`                                                                  | `packages/web/test/signup.test.ts` - enabled and disabled landing/receipt request-link assertions                                                                                                                   |
| F10  | Replaced raw dead-link JSON for browser or unspecified-Accept participant navigation with a generic recovery page linking to `/self-serve/request-link`; retained JSON for explicit API clients and centralized participant-safe no-referrer headers.        | `packages/web/src/participant-http.ts`, `packages/web/src/router/registry.ts`                                                                                            | `packages/web/test/self-serve.test.ts` - expired and revoked link recovery; `packages/web/test/route-registry.test.ts` - human HTML, no-referrer, media-quality, and explicit JSON contracts                        |
| F11  | Reused a shared core parser to accept messy performer-link prose, normalize HTTP(S) URLs, preserve useful residue as a note, drop placeholders, and refuse non-HTTP schemes. The receipt displays the parsed result.                                         | `packages/core/src/participant-links.ts`, `packages/core/src/import/goal1.ts`, `packages/web/src/routes/signup.ts`, `packages/web/src/views/signup-view.ts`              | `packages/core/test/participant-links.test.ts`; `packages/web/test/signup.test.ts` - mixed prose/URLs, placeholder drop, and `javascript:` refusal                                                                  |
| F12  | Rendered queue and participant assignment/change-request timestamps in readable season-local time, with cached formatters and cross-date timezone coverage.                                                                                                  | `packages/web/src/timezone.ts`, `packages/web/src/routes/self-serve.ts`, `packages/web/src/views/admin-records.ts`                                                       | `packages/web/test/self-serve.test.ts` - Chicago local time whose UTC instant is the next date; `packages/web/test/admin-records.test.ts` - readable cross-midnight availability                                    |

## Verification

| Gate                                                        | Result                                                                                                                                                  |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                         | Pass                                                                                                                                                    |
| `npm run lint`                                              | Pass with exactly the two pre-existing `packages/core/src/access.ts` unused-argument warnings                                                           |
| `npm run format:check`                                      | Pass. The four committed UAT evidence documents were mechanically formatted because they otherwise failed the repository-wide gate.                     |
| `npm test`                                                  | Environment-blocked only in `packages/email/test/smtp.test.ts`: the sandbox refuses local `listen()` with `EPERM`. No application test failures remain. |
| `npx vitest run --exclude packages/email/test/smtp.test.ts` | Pass: 51 files, 1000 tests                                                                                                                              |
| `npm run check:boundaries`                                  | Pass                                                                                                                                                    |
| `npm run check:clean-room`                                  | Pass                                                                                                                                                    |
| `git diff --check`                                          | Pass                                                                                                                                                    |

The clean-room self-test that creates a temporary Git repository is also sandbox-blocked (`spawnSync git EPERM`); the actual repository clean-room scan passes.

## Review follow-up

The structured review used correctness, testing, maintainability, security, API-contract, data-migration, learnings, and local adversarial lenses. Network-disabled sandboxing prevented a cross-model peer, so the adversarial pass ran locally. Review-driven follow-ups added:

- absolute and trusted-proxy-aware invite URLs;
- optional unbound organizer invites;
- original snapshot and shared-member preservation during placeholder promotion;
- assigned-slot shared-member prompts;
- pre-0019 upgrade and retention assertions;
- a non-UTC, cross-date timestamp proof;
- a Host/Origin-paired killing test for the Fetch Metadata guard;
- explicit JSON compatibility for participant API callers.

One review suggestion was deliberately rejected: permitting Host-bearing unconfigured mutations without Fetch Metadata would make the required spoofed-Host refusal unverifiable. Programmatic or legacy clients can use a configured `PUBLIC_BASE_URL`; configured deployments retain exact fixed-origin checks.

Review artifact: `/tmp/ce-code-review-uat-20260902-e5ruRp/review.json` (transient diagnostic artifact; this repository report is the durable handoff).

The final simplification pass also consolidated the coordinate verification form, kept the repository's coordinate `Map` representation intact through the view boundary, centralized the organizer sign-in email rule, preserved unanswered original boolean fields as empty, narrowed the invite API input type, and fixed the dead-link response's `Referrer-Policy`. The focused post-simplification run passed 183 tests across seven files.

## Git state and intended commits

No commit, push, or merge occurred because Git metadata is read-only. Once the Git mount is writable, split or coherently group the existing diff using the requested conventional subjects, for example `fix(web): add organizer invite flow (UAT F1)` through `fix(web): render season-local timestamps (UAT F12)`, followed by a documentation-only commit for this report. Re-run the gates after staging the final commit structure.
